# C2/M2 — گزارش بازبینی «Matched M0» (V1.1 — اصلاح Terminology به‌دستور مالک)

وضعیت: **بازبینی کامل — اجرای بک‌تست تازه در این محیط صندوقچه ممکن نیست (MT5 موجود نیست)**
تاریخ: 2026-09-10 — V1.1: اصلاح صریح Terminology به‌دستور مالک (نسبت به V1).
دستور مأموریت مالک: «Matched M0 برای مقایسه معتبر با M2» — فقط مبنای جمعیت + علت واگرایی M0-vs-M2.
بدون هیچ تغییری در کد، آستانه، یا پروداکشن. Freeze همچنان **NOT READY** و ثبت‌شده بدون تغییر.

## ⚠️ اصلاح Terminology (اجباری، به‌دستور مالک)

- اعداد **4782 / 2921 / 1132 / 376 / 169** مربوط به **M2 واقعی** هستند و **به‌هیچ‌وجه نباید به‌عنوان «Matched M0» تلقی شوند.**
- در سراسر این گزارش، آن ستون فقط «M2 واقعی» نامیده می‌شود.
- **Matched M0 مستقل هنوز اجرا نشده است.**
- هیچ نتیجه‌ای دربارهٔ اینکه Matched M0ِ واقعی با M2 برابر یا متفاوت خواهد بود، **صادر نمی‌شود.**

> منابع دست‌اول این گزارش (همه از داده خام راستی‌آزمایی‌شده، نه از اسناد):
> - ران M2: کامیت `0e91848010505f4d32d56ea874d8f17b6c067d4f` (خروجی واقعی، غیر-LFS)
> - ران مرجع M0: برنچ داده `agent-regression-data-real` @ `662a7ec`، پوشه `v2_research_export_v2_cf/`
> - کد فعلی ردیابی‌شده: `arena/01a06d3c:…/v2_research_export_v2_cf.mq5`، SHA-256 `7cbd5c9b8198e9d2…`
> - هر دو گزارش xlsx متاتریدر واقعی هستند (M0: 4,125,474 بایت؛ M2: 4,036,724 بایت) و مستقیماً پارس شدند.

---

## 1) Build / Provenance

| مورد | M2 واقعی | Matched-M0 لازم (هنوز اجرا نشده) | ران مرجع M0 موجود (`_cf`) |
|---|---|---|---|
| سورس اجرایی | build با `SourceCommit=061a1bb4…` (در CSV تصمیمات جاسازی‌شده)، Build=`C2M2_REPAIR_V1` | **همان build اجرای M2** | **قابل اثبات نیست** — هیچ فایل `.mq5` آن ران در ریپو نیست |
| سیاست INVALID در زمان اجرا | **FAIL-OPEN** — 9 ردیف INVALID وارد Step4 شدند (اثبات داده‌ای: Step4 = 376 = 367 ALLOW + 9 INVALID) | FAIL-OPEN همان run | ن/م (در M0 مسیر C2/M2 کلاً غیرفعال است) |
| سورس فعلی ردیابی‌شده | SHA-256 `7cbd5c9b…` = **FAIL-CLOSED** (diff دقیق در `C2_M2_INVALID_FAIL_CLOSED_REPAIR_V1.md`؛ `LATCH_INVALID → return true`) | — | — |

نکته تعیین‌کنندهٔ اثبات‌شده از خود گزارش‌ها: **گزارش xlsx ران M0 موجود ورودی `inp_c2m2_collect_shift1_m0` را ندارد، ولی گزارش M2 دارد** ⇒ ران مرجع M0 موجود با buildی *قدیمی‌تر از* build ابزارسازی C2/M2 اجرا شده؛ بنابراین این M0 **هم‌Build با M2 نیست** (اثبات از sheet ورودی‌های خود گزارش، نه استنباط).

## 2) شناسایی ستون ارسالی — این اعداد «M2 واقعی» هستند

ستونی که پیش‌تر در جدول مقایسه با برچسب «M0 Matched» آمده بود: Setup=4782 و Step به ترتیب **4782 / 2921 / 1132 / 376 / 169**.

بازبینی از فایل‌های خام ران M2:
- `RX Labels M2`: **4,782** ردیف (Setup)
- `PositionOpenDataset M2`: **9,380** لگ؛ توزیع StepNo: 1=4782، 2=2921، 3=1132، 4=376، 5=169
- `C2M2_Step34_Decisions_m2.csv`: **425** دفعه = 367 ALLOW + 49 VETO + 9 INVALID؛ latch=FIRST ×425؛ strict_before فقط در همان 9 ردیف INVALID برابر false (همگی مرزی: `BarCloseTime_Shift1 == T_decision`)

نتیجهٔ قطعی از داده خام: **همان ستون، جمعیتِ ران M2 است.** در این ریپو/برنچ‌ها هیچ ران جداگانه‌ای که بتوان آن را «Matched M0» نامید **وجود ندارد**؛ و طبق دستور اصلاحی مالک: **Matched M0 مستقل هنوز اجرا نشده است.**

## 3) تنظیمات اجرا (از sheet ورودیِ خود هر گزارش)

| ورودی | M2 واقعی (از xlsx) | ران مرجع M0 موجود (از xlsx) | شرط «Matched M0» مالک | ارزیابی M0 موجود در برابر شرط |
|---|---|---|---|---|
| نماد / T/F | XAUUSD / M1 | XAUUSD / M1 | XAUUSD / M1 | ✅ برابر |
| پنجره | 2014.01.01 – 2026.08.19 در هر دو گزارش؛ کات‌اف ران M0 موجود از POSummary: **2026-08-18 23:59:58** = کات‌اف M2 | همان | ✅ برابر |
| مدل | 100% real ticks | 100% real ticks | 100% real ticks | ✅ |
| لورج | 1:100 | 1:100 (همان) | 1:100 | ✅ |
| `inp_cf_mode` | **2** | **0** | 0 | ✅ (فقط همین یک فیلد طبق مأموریت می‌تواند تفاوت داشته باشد) |
| `inp_cf_q75_atr_pct_34` | 0.04668375 | 0.0 | — | در mode=0 غیرفعال است |
| `inp_cf_q75_adx_34` | 47.50305 | 0.0 | — | همانند بالا |
| `inp_cf_q75_atr_pct_23` | 0.0 | 0.0 | 0.0 | ✅ |
| `inp_c2m2_collect_shift1_m0` | false | *(ورودی وجود ندارد)* | **false** | ⚠️ **در build ران M0 موجود اساساً موجود نبوده ⇒ مغایرت Build اثبات‌شده** |
| `inp_reset_setup_id` | true | true | true | ✅ |
| `inp_enable_export` | false | false | false | ✅ |
| `inp_fast_backtest_mode` | **false** | **true** | **false** | ❌ **مغایرت قطعی input** — اثر دقیق آن بر جمعیت **قابل تعیین نیست با شواهد فعلی** |

نتیجهٔ این بخش: M0 مرجع موجود **هم‌Build و هم‌Settings با M2 نیست** (حداقل یک مغایرت input قطعی + یک مغایرت build قطعی).

## 4) جمعیت (نمای فعلیِ داده‌های موجود)

| شاخص | M0 مرجع موجود (`_cf`) | M2 واقعی |
|---|---:|---:|
| Setup (RX Labels) | 4,864 | **4,782** |
| ردیف‌های پوزیشن (PO) | 9,605 | 9,380 |
| سانسور/EOT | M0: فقط ۱ ستاپ `CENSORED_EOT_OPEN` (IsCensored=1)؛ بقیه نهایی‌شده تا cutoff | با دادهٔ موجود بررسی یکسانی انجام نشد؛ جمعیت M2 تا cutoff کامل است (RX/PO/LC به cutoff می‌رسند) |

## 5) جدول Population Parity

| لایه | M0 مرجع موجود | M2 واقعی (= ستون ارسالی) | فاصلهٔ (M0 − M2) |
|---|---:|---:|---:|
| Setup | 4,864 | 4,782 | **+82** |
| Step1 | 4,864 | 4,782 | **+82** |
| Step2 | 2,970 | 2,921 | **+49** |
| Step3 | 1,149 | 1,132 | **+17** |
| Step4 | 431 | 376 | **+55** |
| Step5 | 191 | 169 | **+22** |

## 6) آیا اختلافِ M0 مرجع موجود با M2 قبل از Step4 وجود دارد؟ + علت پله‌به‌پله

**بله — قطعاً وجود دارد.** قبل از هرگونه کد C2/M2:
- −82 در خود Setup/Step1 (پیش از اولین نقطهٔ قرارگیری هر وتو)
- −49 در گذار Step1→Step2
- −17 در گذار Step2→Step3
جمع: **−148 لگ قبل از Step4** که به هیچ وجه قابل انتساب به وتو C2/M2 نیست (ادامه بخش ۷). **علت دقیق این اختلاف هنوز قابل تعیین نیست با شواهد فعلی.**

پلهٔ Step4: فاصلهٔ +55 می‌شکند به: `431 − 49 (VETO واقعی M2) = 382` انتظار با برابری جمعیت؛ تحقق: 376 ⇒ **6 ستاپ دیگر به خاطر تفاوت جمعیت بالادستی نرسیده‌اند** (نه به‌خاطر تصمیم C2/M2). 9 ردیف INVALID در M2 فیلتر نشدند (fail-open در زمان ران) ⇒ اگر fail-closed حاکم بود، Step4 = 367 می‌بود. این اثر فقط در سمت M2 است و در M0 (mode=0) اصلاً فعال نمی‌شود.

پلهٔ Step5: فاصلهٔ +22 ناشی از ترکیب اثر حذف‌شدگان Step4 و تفاوت‌های بالادستی است؛ **تفکیک دقیق کمیّتِ هر جزء: قابل تعیین نیست با شواهد فعلی.** برچسب‌دهی برحسب SetupID برای علت‌های −82/−49/−17 نیز **از دادهٔ فعلی قابل اثبات نیست.**

علت‌های کاندید که فقط تا مرز «قابل‌دفاع» می‌روند (نه بیشتر):
- **اثبات‌شده**: مغایرت build (عدم وجود ورودی C2M2 در گزارش M0 موجود) و `inp_fast_backtest_mode=true` در M0 موجود در مقابل `false` در M2.
- **قابل تعیین نیست با شواهد فعلی**: سهم دقیق هر ورودی/پارچه‌کد در −148 و −22.

## 7) راستی‌آزمایی دامنهٔ C2/M2 (مستقیماً از کد، نه سند)

در سورس ردیابی‌شده (SHA `7cbd5c9b…`):
```c
bool is_c2m2 = (inp_cf_mode == 2 && cur_step == 3 && next == 4);
```
- گیت C2/M2 **فقط** در تصمیم Step3→Step4 صدا زده می‌شود؛ برای سایر پله‌ها مسیر مشترک قدیمی `CF_ShouldVeto` است؛ وقتی `cf_mode==0` و collect خاموش است، هیچ بخشی از منطق C2/M2 اجرا نمی‌شود.
- ترتیب درون‌ران (از کد): گیت‌های بازار/اسپرد ⇒ گیت مارجین ⇒ **آنگاه** وتو ⇒ PlaceEntry.
- قانون: `atr_pct >= τ_ATR && adx >= τ_ADX && DI ناموافق`، با FeatureShift=1، PriceRef=Close[1]، `strict_before = (bar_close < t_decision)`، latch=FIRST.

⇒ **C2/M2 فقط Step3→Step4 را کنترل می‌کند** (راستی‌آزمایی کد/داده هر دو). هیچ اختلافی قبل از آن به C2/M2 منتسب نمی‌شود.

## 8) وضعیت سیاست INVALID

| مقطع | رفتار | شواهد |
|---|---|---|
| کد در لحظهٔ اجرای ران M2 (Build `32795259…`) | **FAIL-OPEN** — INVALID latch می‌شود ولی Step4 را مسدود نمی‌کند | (الف) کد قبل از اصلاح: `... LATCH_INVALID) return false;` (دیف در سند Repair) (ب) دادهٔ خام: 9 INVALID در PO/Step4 ظاهر شدند (376 = 367 + 9) |
| کد فعلی ردیابی‌شده (`7cbd5c9b…`) | **FAIL-CLOSED** — `return true`، INVALID مانع Step4 می‌شود؛ کامنت «Owner final decision» | دیف دقیق در `C2_M2_INVALID_FAIL_CLOSED_REPAIR_V1.md` و کد استخراج‌شده |

فقط گزارش: این امر بر جمعیتِ M2ِ بایگانی‌شده اثر گذاشته است؛ در سمت M0 (mode=0) مسیر C2/M2 غیرفعال است و این سیاست اثری ندارد. هیچ پیشنهاد/تغییری صادر نشده است.

## 9) گزارش P&L (دو لایه، جدا از هم — مخلوط نشده‌اند)

**الف) متریک‌های رسمی MT5 (از گزارش‌های xlsx راستی‌آزمایی‌شده):**

| متریک | M0 مرجع موجود | M2 واقعی |
|---|---:|---:|
| Total Net Profit | **−800.19** | **−1,614.62** |
| Profit Factor | **0.979276** | **0.957556** |
| Gross Profit | 37,811.36 | 36,426.14 |
| Gross Loss | −38,611.55 | −38,040.76 |
| Expected Payoff | −0.0833 | −0.1721 |
| Recovery Factor | −0.2952 | −0.4626 |
| Sharpe Ratio | −0.1328 | −0.2699 |
| Balance DD Maximal | 2,694.54 (0.54%) | 3,473.90 (0.69%) |
| تعداد Trades (در گزارش) | ≈9,605 (Profit Trades ۵۵۳۹ = 57.67% ⇒ مطابق PO) | ≈9,380 (۵۴۳۲ = 57.91% ⇒ مطابق PO) |

**ب) مشتق از دیتاست (جمع `SetupFinalProfit` روی ردیف‌های StepNo=1):**

| | M0 مرجع موجود | M2 واقعی |
|---|---:|---:|
| Σ SetupFinalProfit | **−626.69** | **−1,441.12** |
| Gap با MT5 Net | +173.50 | +173.50 |

توضیح محتمل gap یکسان 173.5 در هر دو ران: اجزای Swap/commission که در جمع‌بندی دیتاست لحاظ نشده‌اند؛ **تطبیق دقیق قابل اثبات نیست با شواهد فعلی.** این دو لایه هرگز در این گزارش با یکدیگر ترکیب نشده‌اند.

## 10) نتیجه‌گیری نهایی (به‌دستور اصلاحی مالک)

**Matched M0 مستقل هنوز اجرا نشده است.**

از داده‌های موجود، آنچه اثبات شده است:

1. **M2 واقعی = 4782 Setup.**
2. **M2 Step1/2/3/4/5 = 4782 / 2921 / 1132 / 376 / 169.**
3. **C2/M2 فقط Step3→Step4 را کنترل می‌کند** (راستی‌آزمایی از کد و داده).
4. **اختلافِ M0 مرجع موجود با M2 قبل از Step4 وجود دارد** (‎−148 لگ پیش از Step4).
5. **علت دقیق این اختلاف هنوز قابل تعیین نیست با شواهد فعلی.**
6. **M0 مرجع موجود هم‌Build و هم‌Settings با M2 نیست** (مغایرت input قطعی `inp_fast_backtest_mode=true≠false` و مغایرت build: نبود ورودی‌های C2M2 در build ران M0 موجود).
7. ستون 4782/2921/1132/376/169 **M2 واقعی است** و نباید به‌عنوان Matched M0 تلقی شود.

**طبق دستور مالک، هیچ نتیجه‌ای دربارهٔ اینکه Matched M0ِ واقعی با M2 برابر یا متفاوت خواهد بود، در این گزارش صادر نشده است.**

## 11) مرحلهٔ بعد (فقط و فقط)

اجرای واقعی **Matched M0** بیرون از این صندوقچه (محیط دارای MT5) با **Build و Settings دقیق M2** و فقط خاموش‌کردن وتو C2/M2:

```
Build  : همان build دارای ابزارسازی C2/M2 (مطابق M2 واقعی — SourceCommit=061a1bb4…)
XAUUSD / M1 / 100% real ticks / D_FULL
Window : 2014-01-01 00:00 → 2026-08-18 23:59:58 (کات‌اف دقیق M2)
Deposit: 500,000 USD / Leverage 1:100
inp_cf_mode=0
inp_c2m2_collect_shift1_m0=false
inp_reset_setup_id=true
inp_enable_export=false
inp_fast_backtest_mode=false
```

هر ورودی‌ای که با M2 واقعی متفاوت باشد باید صریح و جداگانه گزارش شود؛ هیچ ورودی‌ای حدس زده نخواهد شد.

**پایداری سابق**: پروداکشن M0 است؛ Freeze **NOT READY**؛ در این نسخه (V1.1) نیز هیچ Code Change، Threshold Change یا Strategy Change انجام نشده است — تنها Terminology گزارش به‌دستور مالک اصلاح شد.
