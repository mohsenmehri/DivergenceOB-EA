# STRATEGY AUDIT — مستقل از ممیزی فنی (DivergenceOB-EA / XAUUSD)

تاریخ: 2026-09-04 · روش: این سند **فقط** به این سؤال میپردازد: «آیا خودِ استراتژی Edge مالی دارد؟» — نه به سلامت برنامهنویسی (آن در `v2_research_export_v2_AUDIT_REPORT.md` است). هر ادعایی یا مبتنی بر **کد** است یا روی دادههای **واقعاً موجود**؛ هر جا داده نبوده، صریحاً «قابل اثبات نیست» گفته شده.

---

## 0. قاعدهٔ صداقت (قبل از هر عددی)

1. **TRUE_FORWARD واقعاً هرگز اجرا نشده است.** در ممیزی فنی ثابت شد `S2D_HandleTrueForwardOnDeinit`/`S2C_RunPhase*` صفر caller دارند و تنها hook خودکار، بارگذاری frozen در OnInit (فقط وقتی `MODE_TRUE_FORWARD` + فایل frozen موجود باشد) و validation در OnTimer است. بنابراین **هیچ نتیجه‌ای در این ریپو Forward Validation واقعی نیست** و من هم هیچ عددی را این‌طور معرفی نمی‌کنم.
2. **فایل‌های CSV/XLSX/LOG ریپو pointerهای LFS هستند و آبجکت آن‌ها روی سرور وجود ندارد** (تست شد: `info/lfs/objects/batch` → 404 «Object does not exist on the server»؛ دانلود مستقیم هم مسدود است). یعنی Features (14MB)، Labels (1MB)، LifecycleEvents (2.7MB)، PositionOpenDataset (26MB)، Step1_OutcomeComparison (6.2MB)، MarketEdge (55MB) **در دسترس نیستند**.
3. آنچه واقعاً موجود است: `Stats_XAUUSD.txt`، `PositionOpenSummary_XAUUSD.txt` (UTF-16 واقعی)، `SetupID_XAUUSD.txt`، `RX_…_Manifest.json` (schema کامل: 358 ستون feature + 33 ستون label).
4. نتیجهٔ مستقیم: **هر معیار نیازمند PnL/time-series (Expectancy, PF, DD, MAE/MFE, R-multiples, سال‌به‌سال، A/B گروه‌ها) در حال حاضر قابل محاسبه نیست.** این را با اعداد جعلی پر نمی‌کنم.

---

## 1. تنها شواهد تجربیِ در دسترس (و تفسیر دقیقشان)

### 1.1 Stats_XAUUSD.txt
`Opened=4970 | Closed=4970 | NextSetupID=4971` → یک run کامل روی XAUUSD، ۴۹۷۰ setup، همگی بسته‌شده (بدون ردیف censored در این فایل).

توزیع `max_step_reached` (مجموع = 4970): step1=2382 (47.9%)، step2=1561 (31.4%)، step3=631 (12.7%)، step4=221 (4.4%)، step5=175 (3.5%).

**خوانش ساختاری:** فقط ~52% از setuptها به استپ ۲ رسیدند و فقط ~7.9% به استپ ۴/۵. یعنی «نردبان عمیق» یک رخداد نادر است؛ اکثر ستها یا در استپ ۱ تمام می‌شوند یا در استپ ۲–۳. هر ادعای «میانگین‌گیری سنگین پایین» باید به این توزیع محدود شود.

### 1.2 PositionOpenSummary_XAUUSD.txt (شمارش و شرطی‌سازیِ open)
- All Opened Positions: 9156 (ردیف‌های position-open dataset؛ ~1.84 ردیف به ازای هر setup — نشانه‌ی آن است که dataset چند ردیف بازشدن/مرحله دارد، نه ۴۹۷۰ ردیف خالص؛ برای نسبت‌های دقیق نیاز به خود CSV است).
- Setup STOP rows: 352 | Setup LOSS rows: 352 | Setup DEEPSTOP rows: 307 | SUCCESS rows: 8804 (ابهام ذاتی در همین summary: 352+8804≠9156؛ بدون Labels.csv قابل تفکیک نهایی نیست).
- **خوانش محتاطانه:** اگر 352 = setupهای پایان‌یافته با خروج ضرر (SL_DOLLAR/TIME_STOP/…)، نرخ ضرر ≈ 7.1% از ستها؛ و 307 از 352 (≈87%) در `max_step>=4` رخ داده → یعنی **ضررهای این استراتژی تقریباً همیشه «بعد از نردبان عمیق» هستند** (همان الگوی asymmetric که در بخش ۳.۸ بحث می‌شود). این یک مشاهدهٔ ساختاری مهم است اما بدون PnL نمی‌گوید متوسط ضرر چقدر است.

### 1.3 شرطی‌سازی شاخص‌ها در لحظهٔ OPEN (STOP vs SUCCESS) — تنها «سیگنال‌دهی» موجود
| TF | RSI_STOP (med) | RSI_SUCCESS (med) | ATR_STOP (med) | ATR_SUCCESS (med) | CCI_STOP (med) | CCI_SUCCESS (med) |
|---|---|---|---|---|---|---|
| M1 | 29.0 | 45.5 | 0.92 | 0.42 | −154 | −17 |
| M5 | 32.2 | 42.9 | 1.79 | 0.97 | −151 | −91 |
| M15 | 35.1 | 45.9 | 2.77 | 1.64 | −144 | −72 |
| M30 | 38.8 | 47.5 | 3.69 | 2.32 | −124 | −43 |
| H1 | 42.7 | 48.6 | 4.93 | 3.32 | −107 | −20 |
| H4 | 48.5 | 50.3 | 8.70 | 6.53 | −31 | −2 |

**تفسیر (و فقط همین حد):**
- ورودی‌هایی که به STOP ختم شده‌اند، در لحظهٔ باز شدن، RSI واگراییِ ضعیف‌تر (میانه M1: 29 در برابر 45.5)، ATR ~۲ برابر بالاتر (0.92 در برابر 0.42)، و CCI به‌مراتب منفی‌تر داشته‌اند. همین الگو در جدول STEP5 vs NON-STEP5 هم دیده می‌شود.
- این یعنی **متغیرهای لحظهٔ ورود با outcome همبسته‌اند** (شرطی‌سازی معتبر)، اما **نه** اثبات Edge: بدون PnL و بدون کنترل نرخ ورود در هر رژیم نمی‌توان گفت فیلتر RSI/ATR انتظاری مثبت می‌سازد یا فقط تعداد معاملات را کم می‌کند. (جالب: شکاف RSI در TFهای پایین بزرگ و در H4 تقریباً صفر است → هر فیلتر RSI باید در TFهای پایین جست‌وجو شود، نه H4.)
- **هشدار روش‌شناختی:** همین جدول‌ها اگر برای «انتخاب threshold» استفاده شوند، Data Mining روی کل داده است (ممنوع، بخش ۶). فقط به‌عنوان «فرضیه‌ساز» استفاده می‌شوند.

---

## 2. معماری استراتژی از نگاه مالی (نقشهٔ کد)

زنجیره: **DIV3 detect (17,060) → Arm (16,829) → EAP gate-chain (16,943) → FireSignal (17,903) → Setup/Create (19,203) → Ladder (ManagePositions 42,141) → BE/Exit (CheckTakeProfit 42,597 و…) → Finalize**

| مؤلفه | محل کد | نقشی که واقعاً دارد |
|---|---|---|
| Triple Divergence | 17,060–17,320 (گیت‌ها 17,068–17,089) | تولید سیگنال؛ سه پیک بسته (shift≥1) |
| Order Block | `DetectOrderBlocks` 46,697 + ستون‌های OB در snapshot | **فقط feature/ثبت در research؛ در تصمیم ورود DIV3 هیچ gate روی OB پیدا نشد** (EAP 16,957–17,027: structure=شکست/ری‌کلیم سطح P3، نه OB) |
| Entry step1 | 17,914–17,915 + PlaceEntry 19,307 (مارکت‌اوردر) | ورود در اولین تیک بعد از تأیید |
| Ladder 2–5 | 42,152–42,300 | ورود شرطی-سطح + lot فزاینده (۱،۲،۳،۴،۵) |
| BE | weighted 17,963–17,987؛ net-basket BE 42,653–42,696 | قفل سربه‌سر بعد از عمیق‌شدن |
| خروج‌های واقعی | SL_DOLLAR 42,640؛ TIME_STOP 42,711؛ TP nominal (42,895+)؛ EMA200 target؛ TP1_ATR_TRAIL 42,726–42,761؛ DEEP_PROFIT_PROT 42,767–42,787؛ NET_BASKET_BE 42,680 | مجموعه‌خروج |
| Deep Stop | فقط **برچسب پژوهشی** است: `stop_flag && max_step>=4` (18,588/24,107) — خروج مجزای کد نیست | عدم تطابق نام با رفتار |
| ATR Trail | `inp_exit_mode` 923؛ پیش‌فرض `EXIT_LEGACY` | آپشن؛ در run پیش‌فرض استفاده نشده |

**نکتهٔ مهم برای Edge Breakdown:** گروه «B) DIV3+OB» در این کد به معنای یک فیلتر مجزا وجود ندارد — OB فقط در ستون‌های `OBInTol`/`BreakOfStructureFlag`/`OBDistPct` ثبت می‌شود. پس مقایسهٔ A/B/C فقط به‌صورت **post-hoc روی ستون‌های dataset** ممکن است (وقتی داده موجود شود)، نه به‌صورت دو حالت اجرایی متفاوت.

---

## 3. ده سؤال، مؤلفه‌به‌مؤلفه (شواهد = کد / نظریه = ساختار / ناشناخته = داده نیست)

### 3.1 Market Hypothesis هر مؤلفه چیست؟
- **Triple Divergence (نظریه):** سه کف/سقف قیمتی صعودی/نزولی همراه با RSI ضعیف‌تر → «موج سوم ضعف»؛ فرض بر بازگشت یا حداقل توقف رروند. منطقاً Single واگرایی پراشتباه است و Double در بسیاری از سیستم‌ها هنوز «چاقوی در حال سقوط» است؛ سه-پیک = تأخیر بیشتر اما کیفیت بالاتر (کاهش count به‌ازای افزایش precision) — **یک trade-off منطقی، نه یک Edge تضمین‌شده.**
- **OB (شواهد کد):** هیچ فرضیهٔ اجرایی ایفا نمی‌کند (فقط ثبت feature). پس «اضافه‌کردن Edge» از مسیر اجرا **اثبات‌پذیر نیست** — در بهترین حالت داده‌ی پژوهشی برای آینده است.
- **Entry/Ladder:** فرض «ورود در حدِّ واگرایی و میانگین‌گیری پایین‌تر» = میانگین ورود بهتر در ادامهٔ حرکت؛ ریسک = خرید در روند مخالف.
- **BE/TP/Trail:** تبدیل سود شناور به سود قطعی و «نگه‌داشتن بردها پس از استپ ۴ با خروج در BE».

### 3.2 چرا Triple بهتر از Single/Double باشد؟ (نظریه)
استدلال: با RSI سه‌پیک، پیک سوم «ضعیف‌ترین» پالس است و ساختار دو کف قبلی، سطح invalidation مشخص می‌دهد (کمتر از P3 → ابطال، خط 16,960–16,972). **مزیت = نرخ ابطال پایین‌تر؛ هزینه = ورود دیرتر و فاصلهٔ کمتر تا TP. آیا خالصاً مثبت است؟ قابل اثبات نیست** — نیاز به A/B با single/double روی دادهٔ یکسان (هیچ‌کدام در فایل‌های موجود جدا نشده؛ `SignalSource` در featureها می‌تواند بعداً این تفکیک را بدهد اگر runهای مختلف موجود باشند).

### 3.3 OB واقعاً Edge اضافه می‌کند یا فقط فیلتر است؟ (شواهد)
در مسیر ورود: **نه فیلتر است، نه gate** — فقط feature. در research هم چون مدل‌ها (S2B/S2C) dead-codeاند، حتی «اثر OB در امتیازدهی» هم در runهای خودکار اعمال نشده. نتیجه: OB در حال حاضر **صفر تأثیر بر معاملات** دارد؛ هر ادعای «DIV3+OB بهتر است» از خروجی‌های کنونی قابل استنتاج نیست.

### 3.4 آیا Entry فعلی از نظر R:R منطقی است؟
قبل از فیکس E-1: ورود مارکت‌اوردر در اولین تیک بعد از تأیید، درحالی‌که کل هندسه (TP1 ≈ ent0+dist12، BE وزنی، سطوح استپ) روی «حدِّ بار قبل» بسته شده بود → R:R واقعیِ هر ست با R:R برنامه‌ریزی‌شده تفاوت سیستماتیک داشت (فیکس در بخش A: re-anchor پس از fill).
ساختار R:R اسمی: فاصلهٔ استپ‌ها = base × {1,2,3,5,5}، base = ATR(1)×2.5؛ TP1 = ent0 + dist12 (= +2×base اگر mult استپ۲ = 2). با lot {1,2,3,4,5}، میانگین وزنی پر شدن کامل ≈ ent0+8.9×base؛ TP1 از ent0 = +2×base. یعنی **اگر هر ۵ استپ پر شوند، TP1 کوتاه‌تر از میانگین ورودِ سبد است** (خروج سربه‌سر/کوچک تا وقتی قیمت به +2base نرسد فقط استپ ۱–۲ سود می‌دهند؛ سبد کامل برای سود به TP2/3 یا EMA200 نیاز دارد). این «حق‌بگیر» ساختاری است، نه باگ — اما همان چیزی است که توزیع R را به‌شدت چوله می‌کند و باید با R-multiple واقعی اندازه گرفته شود (داده نیست).

### 3.5 Ladder بهتر می‌کند یا فقط avg win/loss را جابه‌جا می‌کند؟ (نظریه + شمارش)
- مکانیک: با ادامهٔ خلاف جهت قیمت، ورودهای سنگین‌تر (lot فزاینده) میانگین را پایین می‌آورد؛ اگر قیمت به BE بازگردد، سبد بدون ضرر می‌بندد (NET_BASKET_BE 42,680). در روند مخالفِ قوی، خسارت = ورودهای بزرگ‌تر در سطوح بدتر → **توزیع ضرر چوله و سنگین‌تر از توزیع سود** مگر آنکه نرخ «بازگشت به BE» بالا باشد.
- شمارش موجود: 87% استاپ‌ها در step≥4 (یعنی پس از سنگین‌ترین ورودها) رخ داده‌اند. این با «لدر، توزیع‌های دم را بزرگ می‌کند» سازگار است. سود/زیان خالص بدون PnL قابل محاسبه نیست. → پاسخ: **به احتمال ساختاری avg-loss را بزرگ‌تر می‌کند؛ اینکه expectancy بهتر شود یا نه، سؤال A/B است (گروه D vs E) و داده نیست.**

### 3.6 BE زودهنگام برنده‌ها را حذف می‌کند؟ (شواهد کد = بله، پتانسیل دارد)
- `DEEP_PROFIT_PROT` (42,767–42,787): وقتی max_step≥4 و floating>0 و قیمت به BE برگردد، کل سبد بسته می‌شود → **هر move بزرگ بعد از استپ ۴ که ابتدا به BE برگردد، قطع می‌شود.** بستن در BE بعد از استپ ۴ یعنی حداکثر سودِ نگه‌داشته ≈ فاصلهٔ BE تا نقطهٔ اوج قبل از بازگشت؛ MFEهای بزرگ بعد از آن از دست می‌روند. همین‌طور `NET_BASKET_BE` بلافاصله در BE می‌بندد.
- تعیین کمّی (چند درصد از MFE واقعی قربانی می‌شود) نیازمند Labels (ستون‌های MFEMoney/MAEMoney/EventPath موجودند اما فایل نیست).

### 3.7 آیا TP/Exit با ماهیت DIV3 سازگار است؟ (نظریه)
DIV3 یک «reversal/swing» سیگنال است؛ TPهای ثابت کوتاه (TP1 ≈ +2base) با نوسان XAUUSD (ATR بالای لحظهٔ ورود در جدول 1.3) احتمالاً خیلی زود لمس می‌شوند و سبدِ سبک را می‌بندند — یعنی استراتژی عملاً «اسکالپ-روی-واگرایی» می‌شود نه «سوینگ-روی-واگرایی»، در حالی‌که نردبان ۵-مرحله‌ای برای سوینگ طراحی شده. **ناسازگاری ساختاری محتمل بین TP کوتاه و لدر عمیق** → نیاز به A/B «EXIT_LEGACY vs TP1_ATR_TRAIL» (همین آپشن موجود است، 923) و مقایسهٔ MFE باقی‌مانده.

### 3.8 Stop/DeepStop عدم‌تقارن ضرر می‌سازد؟ (شواهد + شمارش)
- خروج ضرر واقعی: `SL_DOLLAR` روی **floating خالص کل سبد** (شامل کارمزد/سواپ برآوردی، 42,627–42,648) = ضرر دلاری کران‌دار (~$300 پیش‌فرض). این سقف خوب است.
- اما چون DeepStop فقط برچسب پژوهشی است (max_step≥4 و stop_flag)، «حفاظت ساختاری» در برابر عمیق‌ترین سبدها همان SL_DOLLAR است؛ و چون لدر تا استپ ۵ ادامه می‌یابد، مسیر به سمت −$300 معمولاً از سنگین‌ترین ورودها می‌گذرد → **ریسک/پاداش مسیرِ به‌سمت استاپ با مسیرِ به‌سمت TP یکسان نیست (چولگی منفی).** شمارش: 307/352 استاپ در step≥4. تا وقتی R-multiple واقعی در دسترس نباشد، «چقدر بد» قابل کمّی‌سازی نیست.

### 3.9 تعداد معاملات برای نتیجه‌گیری آماری کافی است؟ (محاسبه)
۴۹۷۰ setup (یا ۹۱۵۶ ردیف position) در یک run. برای یک افکت‌سایز کوچک/متوسط (مثلاً بهبود expectancy به‌اندازهٔ 0.1R با σ≈1.5R): n لازم برای t-test دوطرفه α=0.05, β=0.20 حدود n≈2×(1.96+0.84)²×(1.5/0.1)² ≈ 3500+ → **از نظر تعدادِ خام، ۴۹۷۰ کافی به‌نظر می‌رسد**، اما (الف) ردیف‌ها به‌دلیل سبدهای هم‌پوشان مستقل نیستند (همبستگی سریالی، D-4/S-2 در ممیزی فنی) → حجم مؤثر کمتر؛ (ب) تک‌نماد (XAUUSD) و تک‌بازه → تعمیم‌ناپذیر. نتیجه: برای قضاوت روی همین نماد، N حاشیه‌ای کافی است؛ برای قضاوت استراتژی، خیر (یک نماد).

### 3.10 Edge در رژیم‌های مختلف پایدار است؟ (داده نیست + نکته)
ستون‌های `H1_TrendRegime`، `*_ATRRegime`، `MTFEMA200Align`، `EntryHour`، `Session` در Features موجودند (اما فایل نیست) → تفکیک trend/range و رژیم نوسان و سال‌به‌سال بعد از دریافت دیتا ممکن است. تنها قرینهٔ موجود: ورود در ATR بالا (میزان ۲ برابر) بیشتر به STOP ختم شده — نشانهٔ اولیهٔ آنکه **ادج در رژیم پرنوسان ضعیف‌تر است** (فرضیه، نه نتیجه).

---

## 4. وضعیت تک‌تک معیارهای درخواستی

| معیار | نیازمند | با دادهٔ فعلی |
|---|---|---|
| Expectancy per trade | Labels.FinalProfit | ❌ غیرقابل محاسبه |
| Net Profit / PF | FinalProfit + هزینه‌ها | ❌ |
| Avg/Median Win-Loss | FinalProfit | ❌ |
| Win Rate | Labels.FinalEvent | ⚠️ فقط تقریبی (352/4970 ≈ 7.1% استاپ اگر تفسیر درست باشد) |
| Max DD / Recovery Factor | سری equity | ❌ (MaxDDMoney در Labels است ولی فایل نیست) |
| Payoff ratio / R-multiple | FinalProfit + risk | ❌ |
| MAE / MFE | Labels.MAE/MFE | ❌ |
| Consecutive losses | Labels.CloseTime | ❌ |
| Long vs Short / Session / DOW / سال | ستون‌های زمان/جهت | ❌ (ستون هست، فایل نیست) |
| رژیم نوسان / Trend vs Range | ستون‌های رژیم | ❌ |
| Edge Breakdown A–G | ستون‌های گروه‌ساز | ❌ |

تنها اقلام قابل استخراج هم‌اکنون: توزیع max-step، نرخ خام استاپ/دیپ‌استاپ (با ابهام)، و شرطی‌سازی RSI/ATR/CCI در OPEN (بخش ۱.۳).

---

## 5. Edge Breakdown A–G — دستورالعملِ اجرا (وقتی داده موجود شود)

تعریف گروه‌ها با ستون‌های Manifest (بدون نیاز به run جدید):
- **A) Triple Divergence فقط** = همهٔ ردیف‌های `SignalSource` یکسان (DIV3 confirmed) بدون شرط OB.
- **B) DIV3+OB** = زیرمجموعه با `OBInTol==1` یا `OBDistPct` پایین (threshold فقط روی DEV).
- **C) DIV3+هر فیلتر** = `QCValid==1`، `RSIGatePassed`، ساختار (`BreakOfStructureFlag/CHoCH`)، رژیم (`MTFDIAlign`)، ولت (ATRRegime) — هر کدام جدا.
- **D) بدون لدر** = `MaxStep==1`؛ **E) با لدر** = `MaxStep>=2` (و عمق‌بندی 2/3/4/5).
- **F) Legacy Exit** = ردیف‌های run با `inp_exit_mode=EXIT_LEGACY`؛ **G) New Exit/NetBE/ATR-Trail** = run با `EXIT_TP1_ATR_TRAIL` (نیازمند دو run مجزا با version ثابت؛ `FinalEvent`/`EventPath` جداکنندهٔ NET_BASKET_BE/TRAIL/SL_DOLLAR است).

شرط اعتبار: هر دو run با **نسخهٔ یکسان کد (پس از فیکس‌های بخش A)**، تنظیمات یکسان، نماد/بازهٔ یکسان، state پاک (`inp_reset_setup_id` یا terminal تمیز)، و خروجی با magic یکسان. مقایسه با معیارهای بخش ۴ + تفکیک DEV [قبل از 2023] / OOS [2023–2024] / 2024+ به‌عنوان «دیده‌نشده» (نه TRUE_FORWARD).

---

## 6. ضد Data Mining
- هیچ threshold/filter جدیدی در این سند پیشنهاد نمی‌شود و به EA اضافه نشده است (بخش A فقط باگ‌های تأییدشده را اصلاح کرد؛ هیچ قانون استراتژی جدیدی اضافه نشد).
- اگر بعداً فیلتری آزمایش شود: انتخاب فرضیه/threshold فقط روی DEV (2018–2022)؛ تأیید یک‌باره روی OOS (2023–2024)؛ 2024+ فقط به‌عنوان مشاهدهٔ نهایی. تکرار انتخاب روی OOS ممنوع.
- جدول ۱.۳ (شرطی‌سازی روی کل داده) صرفاً فرضیه‌ساز است و مبنای انتخاب threshold نیست.

---

## 7. پاسخ به سؤال بنیادی: مشکل اصلی این EA چیست؟

| گزینه | پاسخ |
|---|---|
| 1. فقط باگ اجرایی؟ | **خیر.** باگ‌های اجرایی واقعی‌اند (E-1/E-2/E-3 و D-1) و اصلاح شدند، اما توضیح‌دهندهٔ «سود/زیان» نیستند. |
| 2. استراتژی خوب، پیاده‌سازی خراب؟ | پیاده‌سازی بسیار پیچیده و تا حدی ناسازگار است (TP کوتاه × لدر عمیق؛ OB بی‌اثر؛ DeepStop فقط برچسب)، ولی «خوب بودن استراتژی» اثبات‌نشده. |
| 3. Edge ضعیف؟ | **قابل قضاوت نیست** (دادهٔ PnL موجود نیست). |
| 4. Overfit شده؟ | در مسیر اجرا: خیر (مدل‌های S2B/S2C dead-codeاند و در run اعمال نمی‌شوند). در ادعاهای پژوهشیِ دستی: بله/احتمال (AUCهای in-sample، L-1..3). |
| 5. داده/اجرای صحیح برای قضاوت کافی نیست؟ | **بله — پاسخ اصلی.** برای «آیا Edge دارد» دادهٔ لازم (Labels/Features با PnL و زمان) در دسترس نیست و TRUE_FORWARD هرگز اجرا نشده. |

---

## 8. جدول نهایی مؤلفه‌ها

| Component | Evidence of Edge (امروز) | Risk | Verdict |
|---|---|---|---|
| Triple Divergence | نظریهٔ منسجم؛ شرطی‌سازی RSI معتبر؛ بدون PnL | ورود دیر، وابسته به TF پایین، ابطال‌پذیر | **KEEP** (مشروط به شواهد A/B با single/double) |
| Order Block | هیچ تأثیری بر اجرا (فقط feature) | بی‌ضرر اما بی‌اثر؛ ایجاد توهم edge | **MODIFY** (یا gate واقعی، یا حذف از روایت استراتژی؛ نگه‌داشتن در research) |
| Entry | R:R اسمی پس از فیکس E-1 سازگار شد؛ رفتار واقعی بی‌شاهد | مارکت‌اوردر در اولین تیک؛ شکاف با حدِّ واگرایی | **MODIFY** (انجام شد: re-anchor؛ گزینهٔ بعدی limit-order واقعی) |
| Step/Ladder | بدون شاهد؛ ریسک چولگی منفی (87% استاپ‌ها در step≥4) | عمیق‌ترین ورودها = بزرگ‌ترین lotها در بدترین نقطه | **MODIFY** (A/B گروه D vs E و حداکثر-استپ قبل از حذف/تأیید) |
| Break-even | منطق هزینه‌آگاه خوب (net BE)؛ اما قطع زودهنگام بردهای پس از استپ ۴ | DEEP_PROFIT_PROT می‌تواند MFEهای بزرگ را حذف کند | **MODIFY** (اندازه‌گیری MFE پس از خروج؛ تأخیر/حذف protect در A/B) |
| TP/Exit | TP اسمی کوتاه در تضاد احتمالی با لدر عمیق؛ بی‌شاهد | بستن زودهنگام سبد سبک | **MODIFY** (A/B: LEGACY vs TP1_ATR_TRAIL؛ TP بر مبنای ATR پویا) |
| ATR Trail | آپشن موجود، در run پیش‌فرض خاموش (EXIT_LEGACY) | بی‌آزار؛ بی‌شاهد | **KEEP** (آپشن) — فقط با A/B قضاوت می‌شود |
| Deep Stop | در کد خروج نیست؛ فقط برچسب پژوهشی | نام‌گذاری گمراه‌کننده؛ «حفاظت» واقعی = SL_DOLLAR | **MODIFY** (یا خروج واقعیِ مستقل بساز و A/B کن، یا برچسب را صادقانه کن) |

---

## 9. STRATEGY VERDICT

# ⚫ CANNOT BE VALIDLY EVALUATED WITH CURRENT DATA

**دلایل (خلاصه):**
1. فایل‌های خام پژوهشی (Labels با FinalProfit/MAE/MFE، Features با رژیم/سشن/سال) به‌صورت LFS pointer در ریپو هستند و آبجکت آن‌ها روی سرور موجود نیست → expectancy، PF، DD، R-multiple، MAE/MFE، تفکیک A–G و سال‌به‌سال **قابل محاسبه نیست** و من عدد جعلی نمی‌دهم.
2. TRUE_FORWARD هرگز اجرا نشده (صفر caller) → هیچ نتیجه‌ای Forward Validation نیست.
3. تنها یک run، یک نماد (XAUUSD)، بدون PnL → نه می‌توان «HAS EDGE» گفت، نه «WEAK/NO EDGE».

**چه چیزهایی همین امروز اثبات‌شده/قابل اثبات است:**
- ✅ ساختار اجرا پس از فیکس‌های بخش A از نظر ورود/هندسه/حفاظت سازگار است (ممیزی فنی).
- ✅ ۴۹۷۰ setup با توزیع max-step مشخص؛ نرخ خام استاپ ≈7% و 87% از استاپ‌ها پس از استپ ۴.
- ✅ شرطی‌سازی معنادار RSI/ATR/CCI در لحظهٔ ورود با outcome (فرضیه‌ساز).
- ✅ مدل‌های آماری dead-codeاند؛ خطر overfit خودکار در run وجود ندارد.

**برای ارتقا به 🟡/🟢/🔴 دقیقاً این‌ها لازم است:**
1. دریافت/تولید دوبارهٔ `RX_…_Labels.csv` + `Features.csv` + `SetupLifecycleEvents.csv` (همان run یا run مجدد با کد پچ‌شده، state پاک).
2. محاسبهٔ جدول معیارهای بخش ۴ + گروه‌های A–G (دستورالعمل بخش ۵)، تفکیک DEV/OOS/2024+.
3. (برای قضاوت نهایی) دو run A/B با `EXIT_LEGACY` و `EXIT_TP1_ATR_TRAIL`، و اجرای واقعی TRUE_FORWARD با frozen package تا حداقل چند هفته دادهٔ واقعی — آن‌وقت می‌توان برچسب را از ⚫ به یکی از چهار حالت دیگر تغییر داد.

— پایان ممیزی استراتژی. (ممیزی فنی جداگانه: `v2_research_export_v2_AUDIT_REPORT.md`؛ پچ‌های بخش A در working tree فایل mq5 اعمال شده — هنوز در MetaEditor کامپایل نشده.)
