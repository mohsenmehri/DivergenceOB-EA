# PHASE 2 — TECHNICAL REPAIR + INDEPENDENT STRATEGY AUDIT (گزارش نهایی)

- تاریخ: 2026-09-04 · فاز جدا از Full Code Audit قبلی (گزارش آن: `v2_research_export_v2_AUDIT_REPORT.md` — فقط همان Findingهای قفلشده استفاده شده؛ ممیزی از صفر تکرار نشد).
- فایل هدف: `v2_research_export_v2/v2_research_export_v2.mq5` — 51,796 خط (LF)، sha256 `d961dcb9…`، دیف working tree نسبت به HEAD: **+68/−5 در ۵ hunk** (blob e37d28b → 99b7e93).
- هیچ قانون استراتژی، فیلتر یا Threshold جدیدی اضافه نشده؛ هیچ تغییری در Triple Divergence، MTF یا Research schema داده نشده. فقط Confirmed Bugs ممیزی قبلی.
- دادهٔ موجود برای بخش استراتژی: `Stats_XAUUSD.txt` (۴۹۷۰ setup)، `PositionOpenSummary_XAUUSD.txt` (شرطیسازی open)، `SetupID_XAUUSD.txt`، `Manifest.json` (۳۵۸ feature / ۳۳ label). فایلهای CSV/XLSX خام، LFS pointer بدون آبجکت روی سرور هستند (غیرقابل بازیابی اینجا).

---

## 1. Technical Repair Summary

مطابق پروتکل، برای هر تغییر: **Finding → Root Cause → Function → Line → Fix** (خطوط = post-patch):

| Finding | Root Cause | Function | Line | Fix |
|---|---|---|---|---|
| E-1: ورود واقعی با «قرارداد» استراتژی ناسازگار بود | هندسهٔ Step/Ladder و BE/TP و کل Export روی `ent[0]` (اکستریم بار-۱) لنگر شده بود، در حالی که اجرا Market Order در اولین تیکِ بارِ تأیید است → تفاوت fill تا یک رنج بار | `CreateSetupWithPrediction` | ~19,264 | پس از fill واقعی استپ ۱، همهٔ `stop_levels[0..4]`، `break_even_levels[0..3]`، `target_levels[0..4]` و smart-target با `anchor_delta = fill − ent[0]` (در صورت ≥ 1 point) re-anchor شدند؛ پرچمهای EMA200 با fill واقعی بازمحاسبه شدند. بدون Look-ahead: فقط قیمت fillِ گذشته استفاده شده است |
| E-2: هیچ SL محافظتی سمت بروکر وجود نداشت | همهٔ اوردرها با SL/TP=0 ارسال میشوند (مدیریت خروج داخلی)؛ قطع Terminal/EA/Internet = پوزیشن بیحفاظ | `PlaceEntry` + input جدید | 964 (input) / ~19,358 | اینپوت `inp_broker_protection_sl_atr` (پیشفرض 0.0 = خاموش برای برابری بکتست). وقتی >0، بعد از هر fill موفق، پوزیشن تازه (تطبیق Symbol+Magic+Comment) با `PositionModify(ticket, bid|ask ∓ N·ATR, 0)` مجهز به SL پهن فاجعه میشود؛ fallback اگر ATR نامعتبر: point×100؛ failure فقط لاگ میشود (throttle 60s) و مسیر اصلی معامله مختل نمیشود |
| E-3: `Sleep()` بدون قید در مسیرهای معاملاتی | Sleep در Tester رفتار/سرعت وابسته به build دارد | `PlaceEntry`، `ManagePositions`، `CloseSetupWithReason` | 19,422 / 19,428 / 42,311 / 42,895 | هر ۴ سایت با `if(!MQLInfoInteger(MQL_TESTER))` محافظ شد؛ pacing لایو بدون تغییر |
| D-1: حالتهای سیگنالِ بدون معامله، بیصدا no-op بودند | `DIV3_LIVE_P3_ONLY` هرگز معامله نمیکند و `DUAL` فقط جریان تأییدشده را معامله میکند؛ روی کنسول اعلام نمیشد | `OnInit` | ~44,411 | Print صریح شروع: LIVE_P3_ONLY → «NO TRADES — display only»؛ DUAL → «trades confirmed stream only» |

یادداشتهای اصلاحی مهم (بدون تغییر کد): L-1/L-2/L-3 (AUC/کالیبراسیون in-sample و رتبهبندی pooled در گزارشهای پژوهشی دستی) و R-4 (لیترالهای وضعیت) **به عمد پچ نشدند** — متعلق به توابع پژوهشی صفر-caller هستند و اصلاحشان Refactor مرده-کد بود؛ دستور اصلاح در AUDIT_REPORT §17 هست. (در هنگام پچ، تناقض جدیدی با کد پیدا نشد.)

---

## 2. Confirmed Bugs Fixed

| # | شدت قبلی | وضعیت | تغییر رفتار در مسیر پیشفرض |
|---|---|---|---|
| E-1 | 4/5 | ✅ FIXED | فقط وقتی fill واقعی ≥1 point با `ent[0]` فاصله دارد: سطوح و خروجیها به fill واقعی لنگر میشوند (سازگاری R:R/Export). شمارش معاملات و زمانبندی ورود عوض نشده |
| E-2 | 4/5 | ✅ FIXED (پیشفرض خاموش) | با `inp_broker_protection_sl_atr=0` رفتار بکتست عوض نمیشود؛ برای Live باید >0 ست شود (پیشنهاد ≥ 4.0) |
| E-3 | — | ✅ FIXED | در Tester: Sleepها حذف میشوند (سرعت/پایداری)؛ در Live: بدون تغییر |
| D-1 | 2/5 | ✅ FIXED | فقط دو خط Print در OnInit؛ بدون تغییر رفتاری |

پیادهسازی E-2 آگاهانه «Modify پس از Fill» است نه SL هنگام OrderSend: SL داخل OrderSend با خروجهای مدیریتشدهٔ EA (Net-Basket BE استپ≥4، SL دلاری، TP اسمی) رقابت میکند و چون فاصلهٔ محافظ باید «پهنتر از همهٔ خروجهای EA» باشد، بهترین نقطه، لحظهٔ پس از fill با ATR لحظهای است. شکست Modify (رد بروکر و…) = LogWarning با throttle 60 ثانیه؛ fail-safe به این معنا که هرگز مسیر ورود/خروج را بلاک نمیکند.

---

## 3. Compile / Regression Result

- **Compile:** در این سندباکس MetaEditor/wine/MT5 موجود نیست (جستوجو شد) → **کامپایل در اینجا اجرا نشد**. این تنها قید باقیماندهٔ بخش فنی است.
- **Error/Warning check (استاتیک، انجامشده):** تراز آکولاد 0 بدون منفی زودهنگام؛ CRLF=0 / NUL=0؛ یکتایی سایتهای FIX-E1/E2/D1؛ تطبیق ابعاد آرایهها با تعریف `CSetupTracker` (12,578: `stop_levels[5]`/`break_even_levels[4]`/`target_levels[5]`) با حلقههای s<5/b<4/t<5؛ تطبیق امضای `CSmartTarget::Initialize(entry,tp1,tp2,tp3,ema200,target)` (6,956) با فراخوانی 19,280–19,284؛ وجود/الگوی استفادهٔ `GetBufferValue` (مطابق 10,489/10,609)، `LogWarning` (13,408)، `BuildComment` (13,703)، `inp_magic_number` (839، cast به long مجاز)، `CTrade::PositionModify`؛ بازبینی hunk-to-hunk کل دیف.
- **Regression (دستور کار روی دستگاه کاربر):** ۱) کامپایل در MetaEditor (صفر error/warning جدید)؛ ۲) یک بکتست کوتاه با همین تنظیمات روی blob پیش-پچ (e37d28b) و پس-پچ (99b7e93)؛ انتظار: تفاوت فقط در (الف) re-anchor سطوح پس از fill و (ب) حذف Sleepها در Tester؛ شمارش trades/جهتها/زمانبندی باید یکسان باشد.
- **Changed files/lines:** فقط `v2_research_export_v2.mq5` — 5 hunk، +68/−5.
- **Research impact:** هیچ تغییری در schema ستونها/نام فایلها؛ اثر E-1 سودمند است: ستونهای `planned_entry`/گزارشها حالا با `step_prices` واقعی همخوان میشوند (همان خواستهٔ «actual fill مبنای محاسبات بعدی»).

---

## 4. Strategy Hypothesis (استخراجشده از کد، به زبان ساده)

- **بازاری که برایش طراحی شده:** XAUUSD؛ ورود در اولین تیک بعد از بستهشدنِ بارِ حاوی تأیید واگرایی؛ چند-تایمفریم (M1 پایه + M5/H1/H4 برای رژیم/ساختار).
- **Triple Divergence چه میگوید:** سه پیک قیمتی متوالی با شیب ضعیفشوندهٔ RSI → تحلیلگر «خستگی حرکت» را میخواند: فشار فروش (در حالت لانگ) رو به اتمام است و احتمال بازگشت/توقف روند بالا میرود. P3 = آخرین پیک؛ ورود در جهت بازگشتِ فرضی.
- **جهت مورد انتظار:** خلاف شیب قیمتیِ سهپیک (بازگشتی/میانگین-بازگشتی کوتاهمدت)، با شرطهای ساختاری (BOS/ریکلیم سطح) و RSI.
- **Invalidation:** شکست/ادامهٔ زیر P3 (و ساختارِ دو پیک قبلی) — ابطال در ~16,960–16,972.
- **Order Block:** در مسیر اجرا **هیچ نقشی ندارد** — فقط feature پژوهشی (ستونهای OB). یعنی فرضیهٔ «OB بخشی از ورود است» از کد قابل استخراج نیست.
- **Target:** TP اسمی چندسطحی بر پایهٔ ATR (TP1 نزدیک) + آپشن EMA200 هدف؛ در اعماق (استپ≥4) عملاً فقط Net-Basket BE و SL دلاری.
- **R:R واقعی:** به ساختار گرید گره خورده (ATR base × {1,2,3,5,5}، لات {1,2,3,4,5}) و به همین دلیل به fill واقعی وابسته است — پس از فیکس E-1 حالا R:Rِ محاسبهشده با R:Rِ واقعی همخوان است؛ **عدد R:R «طراحیشده» بدون PnL قابل قضاوت نیست** (جزئیات: STRATEGY_AUDIT §3.4).

---

## 5. Strategy Component Audit

خلاصهٔ پاسخ به ۱۱ سؤال برای هر جزء (تفصیل: `v2_research_export_v2_STRATEGY_AUDIT.md` §3):

| جزء | فرضیهٔ بازار | چرا باید Edge داشته باشد | اطلاعات جدید یا صرفاً پیچیدگی؟ | سازگاری با بقیه | ریسک خنثیسازی/هزینه |
|---|---|---|---|---|---|
| **Triple Divergence** | خستگی حرکت پس از ۳ پیک | نرخ ابطال کمتر نسبت به Single/Double (تأخیر بیشتر، کیفیت بالاتر) | هر دو: «تأیید» بیشتر اما ورود دیرتر | مرکزی؛ بقیه حول آن | اگر TP کوتاه باشد، مزیت سوئینگیاش خنثی میشود |
| **Order Block** | (در کد اجرایی وجود ندارد) | — | فقط پیچیدگی/توهم | بیاثر (فقط export) | صفر (اما روایت استراتژی را گمراه میکند) |
| **Entry** | ورود در حدّ واگرایی | پر کردن در بهترین نقطهٔ فرضی | — | پس از E-1 سازگار | Spread/سلیپیج مارکتاوردر واقعی؛ شکاف با حدّ P3 |
| **Step/Ladder** | میانگینگیری پایینتر در ادامهٔ حرکت مخالف | بهبود avg entry اگر بازگشت رخ دهد | complexity بالا | BE/خروجها را پیچیده کرده | **چولگی منفی**: سنگینترین لاتها در بدترین نقاط (۸۷٪ استاپها در step≥4 طبق summary واقعی) |
| **Break-even** | قفل سربهسر پس از عمق | کاهش ضررهای عمیق | — | منطق net-basket هزینهآگاه است | قطع زودهنگام MFEهای بزرگ بعد از استپ ۴ (DEEP_PROFIT_PROT) |
| **TP/Exit** | حرکت پس از DIV3 تا چند base ادامه مییابد | برداشت سود مرحلهای | — | TP کوتاه × لدر عمیق **ناسازگار ساختاری محتمل** | خروج زودهنگام سبدِ سبک؛ بستن استپهای ۱–۲ |
| **ATR Trail** | سود پس از TP1 را دنبال میکند | حفظ روندهای بزرگ | آپشن؛ پیشفرض خاموش (EXIT_LEGACY) | بدون شاهد | — |
| **Deep Stop** | در کد **خروج مجزا نیست**؛ فقط برچسب پژوهشی (max_step≥4 + stop_flag) | — | نامگذاری گمراهکننده | محافظ واقعی فقط SL_DOLLAR (~$300) | عدمتقارن مسیر تا استاپ در برابر مسیر تا TP |

---

## 6. Edge Decomposition

هیچ عددی ساخته نشده (داده نیست). آنچه از کد قابل استنتاج است:

- **A) Triple فقط / C) +فیلترها:** فیلترهای ساختاری (BOS/ریکلیم، رژیم H1 برای فروش، ولت) در مسیر اجرا فعالاند؛ تفکیک «اثر هر فیلتر» فقط با ستونهای موجود (`BreakOfStructureFlag`، `ATRRegime`، `MTFDIAlign` و…) پس از بازیابی CSV ممکن است.
- **B) Triple+OB:** چون OB در اجرا gate نیست، مقایسهٔ A/B فقط post-hoc روی ستونهای OB dataset معنا دارد، نه دو حالت اجرایی.
- **D) بدون Ladder / E) با Ladder:** جداکنندهٔ اجرایی `MaxStep` (ردیفهای step-1-only در برابر عمیقتر)؛ آزمایش روی همان run ممکن است ولی تفسیر علی نیازمند runهای مجزا با حداکثر-استپ قفلشده است.
- **F) Legacy Exit / G) NetBE/ATR-Trail/DeepProfit:** نیازمند دو run A/B با `inp_exit_mode` مختلف (نسخهٔ کد یکسان، post-patch، state پاک).

آزمایشهای دقیق موردنیاز در §13.

---

## 7. Entry / Risk / Reward Audit

- ورود: Market Order در اولین تیک؛ هندسهٔ قدیمی روی اکستریم بار-۱ «داستانی» بود (E-1) و حالا به fill واقعی لنگر شده. **هزینهٔ واقعی هر ورود = Spread + سلیپیج مارکتاوردر + کارمزد/سواپ** که در خروجهای دلاری دیده شده ولی در آمارهای «سود خام» نه — باید از FinalProfit واقعی خوانده شود.
- R:R ساختاری: گرید base=ATR×2.5 با ضریبهای {1,2,3,5,5} و لات {1,2,3,4,5}؛ چون میانگین وزنی ورودِ سبدِ کامل میتواند از فاصله تا TP1 جلوتر باشد، **R:R طراحی با R:R واقعیِ سبدهای عمیق تفاوت دارد** (جزئیات و فرم در STRATEGY_AUDIT §3.4–3.5). تأیید کمّی نیازمند ردیفهای `step_prices` + `FinalProfit`.

---

## 8. Exit / Ladder Audit

- مسیر واقعی: استپهای ۲–۵ ورود سطحی/شرطی با lot فزاینده؛ در `current_step ≥ 4` فقط خروج Net-Basket BE و SL دلاری و TIME_STOP؛ TP اسمی برای سبدهای سبک.
- Ladder در بهترین حالت expectancy را با reshape کردن توزیع عوض میکند، نه با خلق PnL؛ در بدترین حالت exposure را در دمِ مخالف بزرگ میکند (شواهد واقعی موجود: ۳۰۷ از ۳۵۲ استاپ در step≥4 — همان «Deep Stop» برچسبی).
- BE زودهنگام (`DEEP_PROFIT_PROT` 42,767–42,787) میتواند برندههای پس از استپ ۴ را در بازگشت به BE حذف کند؛ اندازهٔ این اثر نیازمند MFE واقعی است.
- Exit/Trail: `EXIT_TP1_ATR_TRAIL` موجود اما در run پیشفرض خاموش؛ سازگاریاش با توزیع حرکت پس از DIV3 ناشناخته (آزمایش F/G).

---

## 9. Overfitting Risk

- **تعداد پارامترها:** زیاد — گیتهای واگرایی (دلتای RSI، فاصلهٔ پیکها، span)، شرطهای ساختار/رژیم، ولت-گیت عددی، فاصله/ضریبهای گرید، پارامترهای BE/Trail/خروج، و در لایهٔ پژوهشی: انتخاب feature (جستوجوی ۲۵۶ زیرمجموعه/ماسک با سایز ۴–۶ از ۸ ویژگی — 32,856/33,005/34,878)، آستانهها (بهینهسازی F2 روی grid 21 نقطهای)، چند-مقایسهای در جستوجوی دستی.
- **مهم:** ماشین S2B/S2C/S2D در مسیر اجرا **dead code** است (صفر caller)؛ یعنی مدل/AUC/threshold منتخب در معاملات واقعی اعمال نمیشود و **overfit خودکارِ deploy شده در بکتست وجود ندارد**. خطر overfit به لایهٔ «گزارشهای پژوهشی دستی + انتخاب آستانهٔ انسانی روی کل داده» منتقل شده (L-1/L-2/L-3 و ریسک data mining روی full dataset).
- **نتیجه:** پیچیدگی استراتژی در لایهٔ اجرا متوسط-بالاست (وابستگیهای متقابل Step/BE/Exit)، اما «بهینهسازی روی داده» در لایهٔ پژوهشی است و تا وقتی داده/پروتکل DEV/OOS رعایت نشود، هر عدد AUC/آستانه فقط توصیف in-sample است.

---

## 10. Research Design

پروتکل الزامی (بدون استثنا):

1. **داده:** بازیابی/تولید دوبارهٔ `RX_…_Labels.csv`، `Features.csv`، `SetupLifecycleEvents.csv` (همان run یا run مجدد با کد post-patch، state پاک، magic یکسان).
2. **DEV (فرضیهسازی و انتخاب):** پنجرههای قبل از ۲۰۲۳ (طبق split کد؛ fallback 70/30 فقط وقتی دادهٔ قبل از ۲۰۲۳ ناقص است) — انتخاب فیلتر/آستانه/گروه فقط اینجا.
3. **OOS (تأیید یکباره):** پنجرهٔ ۲۰۲۳–۲۰۲۴؛ بدون انتخاب مجدد. (در کد: foldهای داخل DEV + OOS-once + latch — مکانیکش سالمه.)
4. **TRUE FORWARD:** **الان واقعاً اجرا نمیشود** (`S2D_HandleTrueForwardOnDeinit` صفر caller؛ فقط بارگذاری frozen package در OnInit وقتی فایل موجود باشد) → **هرگز نتایج DEV/OOS را Forward Validation معرفی نکن**؛ تا اجرای واقعی frozen، این لایه خالی است.
5. معیارهای پذیرش: Expectancy، Net Profit، PF، Payoff، Avg/Median Win-Loss، Win Rate، MaxDD، Recovery Factor، توزیع R، MAE/MFE، Consecutive Losses، تفکیک Long/Short، Session، روز هفته، رژیم نوسان، Trend/Range، سالبهسال — همگی از Labels/Features (ستونها موجودند).
6. **Multiple comparisons:** هر انتخاب threshold روی OOS = ابطال آن آزمایش؛ ثبت تعداد کل آزمایشها.

---

## 11. What Is Actually Proven

- ✅ از کد: صحت زمانی سیگنال (بدون Look-ahead تأییدشده)، عدم double-trade در DUAL، نبود OOS پنهان و نبود self-training خودکار در مسیر اجرا؛ ورود/هندسه پس از E-1 به fill واقعی لنگر شده؛ E-2 محافظ بروکری (وقتی فعال)؛ E-3/D-1 رفع شد.
- ✅ از فایلهای واقعی: ۴۹۷۰ setup (همه بسته) در XAUUSD با توزیع max_step (step1=47.9٪ … step5=3.5٪)؛ نرخ خام استاپ ≈7٪ و غلبهٔ استاپها در step≥4؛ شرطیسازی معنادار RSI/ATR/CCI در لحظهٔ open بین STOP و SUCCESS (فرضیهساز، نه اثبات).
- ❌ **سوددهی/Edge اثبات نشده و اثباتناشدنی با دادهٔ فعلی.**

---

## 12. What Is Still Unknown

- تمام معیارهای PnL (بخش ۳ از STRATEGY_AUDIT: Expectancy، PF، DD، R-multiple، MAE/MFE، …) — فایلهای خام در دسترس نیستند.
- سهم واقعی هر جزء (A–G)؛ پایداری رژیم/سال؛ رفتار Live (سلیپیج، پر شدن جزئی، قابلیت اطمینان PositionModify نزد بروکر واقعی)؛ تعمیم به غیر-XAUUSD؛ نتیجهٔ TRUE_FORWARD واقعی.

---

## 13. Required Experiments

1. رگرسیون فنی: کامپایل MetaEditor + بکتست کوتاه e37d28b در برابر 99b7e93 (بخش ۳).
2. بازتولید دیتاست: run مجدد کد پچشده (CONFIRMED_ONLY، state پاک) یا بازیابی LFS از سرور صاحب ریپو.
3. محاسبهٔ جدول کامل معیارها (بخش ۱۰.۵) روی DEV؛ برچسبگذاری صریح «DEV فقط».
4. Edge Decomposition: گروههای A–G (بخش ۶) روی همان ردیفها؛ هیچ آستانهای روی کل داده.
5. A/Bهای اجرایی: Ladder-DISABLED در برابر ENABLED؛ `EXIT_LEGACY` در برابر `EXIT_TP1_ATR_TRAIL`؛ محافظ E-2 خاموش/روشن (هزینهٔ Modify).
6. OOS-once روی ۲۰۲۳–۲۰۲۴ برای فقط ۱–۲ فرضیهٔ برندهٔ DEV.
7. TRUE_FORWARD واقعی: بستهٔ frozen + چند هفته دادهٔ جدید (تنها در این مرحله برچسب Forward مجاز است).

---

## 14. Final Strategy Verdict

**STRATEGY VERDICT: ⚫ CANNOT BE VALIDLY EVALUATED WITH CURRENT DATA**

- سوددهی/Edge **قابل اثبات نیست** و اثبات نشده؛ هیچ ادعای سوددهی از کد بهتنهایی ساخته نشده است.
- علت: نبود دادهٔ PnL (CSVهای خام = LFS pointer بدون آبجکت)، نبود TRUE_FORWARD واقعی، تک-نماد/تک-run، و اجزای بدون شواهد تجربی (OB بیاثر، Deep Stop برچسبی، TP×Ladder نامطمئن).
- این ⚫ به معنی «بد نیست» هم نیست — یعنی **قضاوت معتبراً ممکن نیست** تا آزمایشهای §13 اجرا شوند. اگر دادهٔ معتبر برسد و اجزای A–G شواهد منفی نشان دهند، برچسب بدون تعارف به 🟠/🔴 تغییر میکند.

---

## Verdictهای نهایی (دوگانه)

### TECHNICAL VERDICT

**READY FOR RESEARCH** — *مشروط به قید اجباری:* کامپایل موفق در MetaEditor + run رگرسیونی بخش ۳ (در این سندباکس ابزار کامپایل نبود؛ تمام QA استاتیک پاس شده).  
**NOT READY FOR LIVE** تا: فعالسازی E-2 با مقدار محافظ مناسب، چند هفته forward واقعی با mode صحیح (CONFIRMED_ONLY)، و تأیید رفتار Modify نزد بروکر واقعی.

### STRATEGY VERDICT

**⚫ CANNOT BE VALIDLY EVALUATED WITH CURRENT DATA**

(اگر دادهٔ معتبر PnL فراهم شود، همین سند مسیر ارتقا به 🟢/🟡/🟠/🔴 را دارد — بخشهای ۱۰ و ۱۳.)

— پایان گزارش Phase 2. اسناد پشتیبان: `v2_research_export_v2_AUDIT_REPORT.md` (فنی، کامل)، `v2_research_export_v2_STRATEGY_AUDIT.md` (استراتژی، تفصیلی)، `notes_audit/sweep_status.md` (دفترچهٔ شواهد).
