# C2/M2 — گزارش بازبینی «Matched M0» (V1)

وضعیت: **بازبینی کامل — اجرای بک‌تست تازه در این محیط صندوقچه ممکن نیست (MT5 موجود نیست)**
تاریخ: 2026-09-10
دستور مأموریت مالک: «Matched M0 برای مقایسه معتبر با M2» — فقط مبنای جمعیت + علت واگرایی M0-vs-M2.
بدون هیچ تغییری در کد، آستانه، یا پروداکشن. Freeze همچنان **NOT READY** و ثبت‌شده بدون تغییر.

> منابع دست‌اول این گزارش (همه از داده خام راستی‌آزمایی‌شده، نه از اسناد):
> - ران M2: کامیت `0e91848010505f4d32d56ea874d8f17b6c067d4f` (خروجی واقعی، غیر-LFS)
> - ران مرجع M0: برنچ داده `agent-regression-data-real` @ `662a7ec`، پوشه `v2_research_export_v2_cf/`
> - کد فعلی ردیابی‌شده: `arena/01a06d3c:…/v2_research_export_v2_cf.mq5`، SHA-256 `7cbd5c9b8198e9d2…`
> - هر دو گزارش xlsx متاتریدر واقعی هستند (M0: 4,125,474 بایت؛ M2: 4,036,724 بایت) و مستقیماً پارس شدند.

---

## 1) Build / Provenance

| مورد | M2 | Matched-M0 لازم | ران مرجع M0 موجود (`_cf`) |
|---|---|---|---|
| سورس اجرایی | build با `SourceCommit=061a1bb4…` (در CSV تصمیمات جاسازی‌شده)، Build=`C2M2_REPAIR_V1` | **همان build اجرای M2** | **قابل اثبات نیست** — هیچ فایل `.mq5` آن ران در ریپو نیست |
| سیاست INVALID در زمان اجرا | **FAIL-OPEN** — 9 ردیف INVALID وارد Step4 شدند (اثبات داده‌ای: Step4 = 376 = 367 ALLOW + 9 INVALID) | FAIL-OPEN همان run | ن/م (در M0 مسیر C2/M2 کلاً غیرفعال است) |
| سورس فعلی ردیابی‌شده | SHA-256 `7cbd5c9b…` = **FAIL-CLOSED** (diff دقیق در `C2_M2_INVALID_FAIL_CLOSED_REPAIR_V1.md`؛ `LATCH_INVALID → return true`) | — | — |

نکته تعیین‌کنندهٔ اثبات‌شده از خود گزارش‌ها: **گزارش xlsx ران M0 ورودی `inp_c2m2_collect_shift1_m0` را ندارد، ولی گزارش M2 دارد** ⇒ ران مرجع M0 با buildی *قدیمی‌تر از* build ابزارسازی C2/M2 اجرا شده؛ بنابراین این M0 یک «Matched M0 هم‌build با M2» **نیست** (اثبات از sheet ورودی‌های خود گزارش، نه استنباط).

## 2) داده‌محوریِ ستون «M0 Matched» کاربر — یافته قطعی

ستونی که در جدول مقایسه به‌عنوان «M0 Matched» آمده بود: Step به ترتیب **4782 / 2921 / 1132 / 376 / 169**، Setup=4782.

بازبینی از فایل‌های خام ران M2:
- `RX Labels M2`: **4,782** ردیف (Setup)
- `PositionOpenDataset M2`: **9,380** لگ؛ توزیع StepNo: 1=4782، 2=2921، 3=1132، 4=376، 5=169
- `C2M2_Step34_Decisions_m2.csv`: **425** دفعه = 367 ALLOW + 49 VETO + 9 INVALID؛ latch=FIRST ×425؛ strict_before فقط در همان 9 ردیف INVALID برابر false (همگی مرزی: `BarCloseTime_Shift1 == T_decision`)

نتیجه: **ستون «M0 Matched» دقیقاً برابر جمعیت خودِ ران M2 است.** هیچ ران جداگانه‌ای با نام «Matched M0» در ریپو/برنچ‌ها وجود ندارد. بنابراین اثبات شده که آن ستون نام‌گذاری اشتباهی از **دادهٔ خود M2** بوده (و تفاوت‌های پیش از Step4 نسبت به M0 واقعی را پنهان می‌کند). این یافته مستقل از هر استدلال کدی است.

## 3) تنظیمات اجرا (از sheet ورودیِ خود هر گزارش)

| ورودی | M2 (از xlsx) | مرجع M0 موجود (از xlsx) | شرط «Matched M0» مالک | ارزیابی |
|---|---|---|---|---|
| نماد / T/F | XAUUSD / M1 | XAUUSD / M1 | XAUUSD / M1 | ✅ برابر |
| پنجره | 2014.01.01 – 2026.08.19 در هر دو گزارش؛ کات‌اف ران M0 از POSummary: **2026-08-18 23:59:58** = کات‌اف M2 | همان | ✅ برابر |
| مدل | 100% real ticks | 100% real ticks | 100% real ticks | ✅ |
| سپرده / لورج | 500,000 USD؟(Currency USD) / 1:100 | / 1:100 (همان) | 500k / 1:100 | ✅ (مبلغ دقیق سپرده در CSV خلاصه M0 ثبت است؛ گزارش xlsx فقط Leverage را رشته‌ای صادر کرده — توسط همان سلول‌ها تطبیق داده شد) |
| `inp_cf_mode` | **2** | **0** | 0 | طبق مأموریت فقط همین یک فیلد می‌تواند تفاوت داشته باشد |
| `inp_cf_q75_atr_pct_34` | 0.04668375 | 0.0 | — | در mode=0 غیرفعال است؛ M0 قدیمی مقدار 0 داشته |
| `inp_cf_q75_adx_34` | 47.50305 | 0.0 | — | همانند بالا |
| `inp_cf_q75_atr_pct_23` | 0.0 | 0.0 | 0.0 | ✅ |
| `inp_c2m2_collect_shift1_m0` | false | *(ورودی وجود ندارد)* | **false** | ⚠️ در build مرجع M0 اساساً موجود نبوده ⇒ **مغایرت build اثبات‌شده** |
| `inp_reset_setup_id` | true | true | true | ✅ |
| `inp_enable_export` | false | false | false | ✅ |
| `inp_fast_backtest_mode` | **false** | **true** | **false** | ❌ **مغایرت قطعی input بین ران مرجع M0 و شرط مقایسه** — این فلاگ ممکن است رفتارهای داخلی جمع‌آوری خاموشی را تغییر دهد؛ اثر دقیق آن بر جمعیت **قابل تعیین نیست با شواهد فعلی.** |

## 4) جمعیت (نمای مطابق)

| شاخص | M0 مرجع (`_cf`) | M2 | ستون «Matched» کاربر |
|---|---:|---:|---:|
| Setup (RX Labels) | 4,864 | 4,782 | **4,782** |
| ردیف‌های پوزیشن (PO) | 9,605 | 9,380 | (9,380) |
| Completed / Open / EOT | چک EOT: فقط 1 ستاپ سانسورشده (`CENSORED_EOT_OPEN`) در M0 — همان یکی که `IsCensored=1` دارد؛ در M2 هم ساختار مشابه (جمعیت نهایی‌شده تا cutoff) | — | — |

## 5) جدول Population Parity

| لایه | M0 مرجع | «Matched» ستون کاربر | M2 raw | فاصلهٔ M0−«Matched» |
|---|---:|---:|---:|---:|
| Setup | 4,864 | 4,782 | 4,782 | **+82** |
| Step1 | 4,864 | 4,782 | 4,782 | **+82** |
| Step2 | 2,970 | 2,921 | 2,921 | **+49** |
| Step3 | 1,149 | 1,132 | 1,132 | **+17** |
| Step4 | 431 | 376 | 376 | **+55** |
| Step5 | 191 | 169 | 169 | **+22** |

**تفسیر داده‌ای قطعی:** پارتی ستون «Matched» با M2 **صفر** است و با M0 واقعی از همین لحظهٔ Setup مغایر. جمعیت واقعی M0 (4,864/9,605) با هر ستون 4782-محور که بنام «Matched M0» ارائه شود، جزو یک ران نیست.

## 6) آیا اختلاف قبل از Step4 پابرجاست؟ + علت پله‌به‌پله

**بله — قطعاً پابرجاست.** قبل از هرگونه کد C2/M2:
- −82 در خود Setup/Step1 (پیش از اولین نقطهٔ قرارگیری هر وتو)
- −49 در گذار Step1→Step2
- −17 در گذار Step2→Step3
جمع: **−148 لگ قبل از Step4** که به هیچ وجه قابل انتساب به وتو C2/M2 نیست (ادامه بخش ۷).

پلهٔ Step4: فاصلهٔ +55 می‌شکند به: `431 − 49 (VETO واقعی M2) = 382` انتظار با برابری جمعیت؛ تحقق: 376 ⇒ **6 ستاپ دیگر به خاطر تفاوت جمعیت بالادستی از راه نرسیده‌اند** (نه به‌خاطر تصمیم C2/M2). 9 ردیف INVALID در M2 فیلتر نشدند (fail-open در زمان ران) ⇒ اگر fail-closed حاكم بود، Step4 = 367 می‌بود. این اثر فقط در سمت M2 است و در M0 (mode=0) اصلاً فعال نمی‌شود.

پلهٔ Step5: فاصلهٔ +22 ناشی از ترکیب اثر حذف‌شدگان Step4 (قطع زنجیرهٔ ستاپ‌های وتو‌شده و جمعیت کوچک‌تر نمایندهگاه) و تفاوت‌های بالادستی است؛ **تفکیک دقیق کمیّتِ هر جزء با دادهٔ موجود: قابل تعیین نیست با شواهد فعلی.** هیچ برچسب‌دهی برحسب SetupID برای علت‌های بالادستی –82/–49/–17 نیز **از دادهٔ فعلی قابل اثبات نیست** (دیتاست مرجع M0 از build دیگری است و صف جریان یکسان نبوده).

علت‌های کاندید که فقط تا مرز «قابل‌دفاع» می‌روند (نه بیشتر):
- **اثبات‌شده**: مغایرت build (عدم وجود ورودی C2M2 در گزارش M0) و `inp_fast_backtest_mode=true` در M0 در مقابل `false` در M2.
- **قابل تعیین نیست با شواهد فعلی**: سهم دقیق هر ورودی/پارچه‌کد در −148 و −22.

## 7) راستی‌آزمایی دامنهٔ C2/M2 (مستقیماً از کد، نه سند)

در `v2cf.mq5` (SHA `7cbd5c9b…`):
```c
bool is_c2m2 = (inp_cf_mode == 2 && cur_step == 3 && next == 4);
```
- گیت C2/M2 **فقط** در تصمیم Step3→Step4 صدا زده می‌شود؛ برای سایر پله‌ها مسیر مشترک قدیمی `CF_ShouldVeto` است؛ وقتی `cf_mode==0` و collect خاموش است، هیچ بخشی از منطق C2/M2 اجرا نمی‌شود.
- ترتیب درون‌ران (از کد): گیت‌های بازار/اسپرد ⇒ گیت مارجین ⇒ **آنگاه** وتو ⇒ PlaceEntry. یعنی هیچ وتو پنهانِ قبل از گیت‌ها وجود ندارد.
- قانون: `atr_pct >= τ_ATR && adx >= τ_ADX && DI ناموافق`، با FeatureShift=1، PriceRef=Close[1]، `strict_before = (bar_close < t_decision)`، latch=FIRST.

⇒ دامنهٔ C2/M2 دقیقاً **فقط Step3→Step4** است (راستی‌آزمایی کد/داده هر دو). هر اختلافی قبل از آن نمی‌تواند به C2/M2 منتسب شود.

## 8) وضعیت سیاست INVALID

| مقطع | رفتار | شواهد |
|---|---|---|
| کد در لحظهٔ اجرای ران M2 (Build `32795259…`) | **FAIL-OPEN** — INVALID latch می‌شود ولی Step4 را مسدود نمی‌کند | (الف) کد قبل از اصلاح: `... LATCH_INVALID) return false;` (diff در سند Repair) (ب) دادهٔ خام: 9 INVALID در Step4/PO ظاهر شدند (376 = 367 + 9) |
| کد فعلی ردیابی‌شده (`7cbd5c9b…`) | **FAIL-CLOSED** — `return true`، INVALID مانع Step4 می‌شود؛ کامنت «Owner final decision» | دیف دقیق در `C2_M2_INVALID_FAIL_CLOSED_REPAIR_V1.md` و کد استخراج‌شده |

نکته گزارش‌محور (بدون حکم): هر مقایسهٔ آیندهٔ M2 با matched-M0 باید این را لحاظ کند که «جمعیتِ M2ِ بایگانی‌شده» با سیاست fail-open ساخته شده؛ تکرارِ M2 با build فعلی (fail-closed) جمعیتِ Step3→4 متفاوتی خواهد داد (9 ستاپ متأثر: Step4=367). **در سمت M0 این مسئله اثری ندارد** (مسیر C2/M2 در mode=0 غیرفعال است).

## 9) گزارش P&L (دو لایه، جدا از هم — مخلوط نشده‌اند)

**الف) متریک‌های رسمی MT5 (از گزارش‌های xlsx راستی‌آزمایی‌شده):**

| متریک | M0 مرجع | M2 |
|---|---:|---:|
| Total Net Profit | **−800.19** | **−1,614.62** |
| Profit Factor | **0.979276** | **0.957556** |
| Gross Profit | 37,811.36 | 36,426.14 |
| Gross Loss | −38,611.55 | −38,040.76 |
| Expected Payoff | −0.0833 | −0.1721 |
| Recovery Factor | −0.2952 | −0.4626 |
| Sharpe Ratio | −0.1328 | −0.2699 |
| Balance DD Maximal | 2,694.54 (0.54%) | 3,473.90 (0.69%) |
| تعداد Trades (در گزارش) | ≈9,605 (Profit Trades ۵۵۳۹ = 57.67% ⇒ مطابق PO) | ≈9,380 (۵۴۳۲ = 57.91% ⇒ مطابق PO) |

**ب) مشتق از دیتاست (جمع `SetupFinalProfit` روی ردیف‌های StepNo=1):**

| | M0 مرجع | M2 |
|---|---:|---:|
| Σ SetupFinalProfit | **−626.69** | **−1,441.12** |
| Gap با MT5 Net | +173.50 | +173.50 |

gap یکسان 173.5 در هر دو ران: توضیح محتمل = اجزای Swap/ commission که در جمع‌بندی دیتاست لحاظ نشده‌اند؛ **تطبیق دقیق معامله‌به‌معامله قابل اثبات نیست با شواهد فعلی.** این دو لایه هرگز در این گزارش با یکدیگر ترکیب نشده‌اند.

> اعداد کامل خانواده TP/Basket-BE/Dollar-SL برای M0 مرجع در `Stats/POSummary` برنچ داده موجود است (وضعیت‌بندی: ۵۰/۴۳/… فایل Stats)؛ برای M2 باید از `Stats_XAUUSD.txt` کامیت `0e91848` استخراج شود که در این نسخه گزارش به تفصیل لازم نیست — اگر بخواهید در چرخه بعدی کامل درج می‌شود.

## 10) نتیجه‌گیری نهایی (Final Verdict)

**سؤال مالک: آیا «M0 Matched» اکنون مبنای معتبری برای مقایسه با M2 است؟ پاسخ: خیر.**

دلایل قطعی (همه از داده/کد اثبات‌شده):
1. **آنچه «Matched M0» نامیده شده است، خودِ جمعیتِ ران M2 است** (تطابق ردیف‌به‌ردیف شامل 376 در Step4 که فقط با وتو C2/M2 تولید می‌شود؛ یک M0 واقعی با مسیرِ وتوِ خاموش نمی‌تواند 376 را تولید کند — آن‌گاه Step4 از 431 آغاز می‌شد). هیچ artifact جداگانه‌ای با ساختار «matched» در ریپو وجود ندارد.
2. **تفاوت‌ها قبل از Step4 هم پابرجاست**: −148 لگ از M0 مرجع تا پیش از Step4، که به C2/M2 غیرقابل انتساب است؛ علت دقیق آن **قابل تعیین نیست با شواهد فعلی** (و اصالت‌سنجی build ران مرجع M0 ممکن نیست: سورس اجرایی‌اش در ریپو نیست).
3. **مغایرت input قطعی وجود دارد**: مرجع M0 دارای `inp_fast_backtest_mode=true` است در حالی که شرط مقایسه `false` است؛ ورودی‌های C2M2 (مخصوص build دارای ابزارسازی) اصلاً در آن run وجود ندارند ⇒ هم‌build نیست.
4. **اجرای ران تازه در این صندوقچه ممکن نیست** (نبود MT5/MQL5/هیستوری تیک). بنابراین جمعیتِ «Matched M0» واقعی **هنوز وجود ندارد** و بافاصلهٔ قطعی گزارش می‌شود که: *برای حصول مبنای معتبر، یک ران بک‌تست بیرونی لازم است* با: همان build دارای ابزارسازی C2/M2 (Fail-Open برای وفاداری به جمعیت M2ِ بایگانی‌شده؛ اگر تصمیم مالک بر وفاداری به build فعلیِ fail-closed بود، جمعیت M2 نیز باید مجدد تولید گردد) و ورودی‌های برابر با شرط مالک (`cf_mode=0`، collect=false، reset=true، export=false، **fast=false**، پنجره‌ی المحدود مانند M2 با cutoff: 2026-08-18 23:59:58، XAUUSD M1، 100% real ticks، 500k، 1:100، D_FULL). هر ورودیِ متفاوت با این فهرست باید صریح گزارش شود.

**پایداری سابق**: پروداکشن M0 است؛ Freeze **NOT READY**؛ هیچ‌کدام از مناطق ممنوعه (تغییرکد، آستانه، استراتژیک، FE، تیونینگ، حذف ستاپ، edge-verdict) لمس نشده‌اند.
