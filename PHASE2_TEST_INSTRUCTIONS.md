# PHASE 2 — راهنمای تست (فایلهای اصلاحشده)

تاریخ: 2026-09-04 · شاخه: `arena/01a06af7-divergenceob-ea`

## ۱) فایل اصلی برای تست

| فایل | sha256 |
|---|---|
| `v2_research_export_v2/v2_research_export_v2.mq5` (اصلاحشده — Phase 2A) | `d961dcb9113749aa8a814fd88e60a87075e66c27094a239b1e0e18d9d9bc99f6` |

نسخهٔ قبل از اصلاح (برای مقایسه): commit `591967b5fa397c975de7e10154cfa613bee8bf4f` — همان مسیر فایل.

## ۲) چه چیزی اصلاح شده (فقط Confirmed Bugs؛ بدون قانون/فیلتر/Threshold جدید)

| فیکس | محل (خطوط پس از پچ) | اثر |
|---|---|---|
| E-1 | `CreateSetupWithPrediction` ~19,264 | سطوح Step/BE/TP و smart-target بعد از fill واقعی استپ ۱ با `anchor_delta = fill − ent[0]` دوباره لنگر میشوند (هندسه/R:R/Export با ورود واقعی هماهنگ). بدون Look-ahead. |
| E-2 | اینپوت 964 + `PlaceEntry` ~19,358 | اینپوت `inp_broker_protection_sl_atr` (پیشفرض 0.0 = خاموش). اگر >0 باشد بعد از هر fill یک SL پهن محافظتی سمت بروکر (N×ATR) با `PositionModify` گذاشته میشود؛ شکست فقط لاگ (throttle 60s). |
| E-3 | 19,422 / 19,428 / 42,311 / 42,895 | هر ۴ سایت `Sleep()` مسیر معاملات با `if(!MQLInfoInteger(MQL_TESTER))` محافظ شد. |
| D-1 | `OnInit` ~44,411 | Print شروع: `DIV3_LIVE_P3_ONLY` هیچ معاملهای باز نمیکند؛ `DUAL` فقط جریان تأییدشده را معامله میکند. |

## ۳) مراحل تست (روی سیستم خودتان)

1. **دانلود:** فایل بالا را از همین شاخه بردارید و sha256 را چک کنید.
2. **کامپایل:** در MetaEditor باز کنید → Compile → **صفر error** (نسبت به نسخهٔ 591967b نباید warning جدید اضافه شود).
3. **رگرسیون (بکتست مقایسهای):** همان نماد (XAUUSD)، همان بازه، همان تنظیمات ورودی (پیشفرضها)، state پاک — یک بار با فایل قدیمی (591967b) و یک بار با فایل جدید. انتظار: شمار trades/جهتها/زمانبندی یکسان؛ تفاوت مجاز فقط: (الف) سطوح گرید/گزارشها به fill واقعی لنگر شدهاند (E-1)، (ب) Sleep در Tester اجرا نمیشود (E-3).
4. **پیشنهاد پژوهشی:** با modeِ `DIV3_CONFIRMED_ONLY` و ورودیهای پیشفرض تست کنید.

## ۴) نکات Live (وقتی خواستید زنده تست کنید)

- `inp_broker_protection_sl_atr` را > 0 بگذارید (پیشنهاد ≥ 4.0) تا SL فاجعهٔ سمت بروکر فعال شود.
- EA هنوز برای Live بدون نظارت **READY نیست** تا forward واقعی و تأیید رفتار Modify نزد بروکر انجام شود (گزارش Phase 2 را ببینید).

## ۵) اسناد مرتبط (همین شاخه)

- `v2_research_export_v2_PHASE2_REPORT.md` — گزارش نهایی Phase 2 (ساختار ۱۴بخشی + دو رأی فنی/استراتژی)
- `v2_research_export_v2_STRATEGY_AUDIT.md` — ممیزی مستقل استراتژی (تفصیلی)
- `v2_research_export_v2_AUDIT_REPORT.md` — گزارش کامل ممیزی فنی کد (شامل §17 فیکسها)
